{
  "name": "mongodb-todo",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:vergilius/mongodb-todo.git",
  "author": "<michal.pawlinski@arteverest.pl>",
  "license": "GPLv3",
  "private": true,
  "scripts": {
    "run-server": "superstatic --port=8080 --compression --gzip --config=superstatic.json",
    "js-typecheck": "yarn flow",
    "js-unit": "yarn jest --coverage",
    "test": "yarn js-typecheck && yarn js-unit",
    "watch": "yarn webpack --watch & yarn run-server"
  },
  "devDependencies": {
    "flow-bin": "^0.113.0",
    "flow-typed": "^2.6.2",
    "jest": "^24.9.0",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.8.9"
  }
}
